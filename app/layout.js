import './globals.css';

export const metadata = {
  title: 'Workforce Lens — Staffing Capacity & Coverage Lab',
  description: 'Forecast staffing coverage, detect capacity risk, and plan workload rebalancing without employee scoring.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
